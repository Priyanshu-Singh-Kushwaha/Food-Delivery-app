specVersion: "v1"
serviceId: "nexux"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nexux-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
